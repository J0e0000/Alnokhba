# النخبة — سجل التغييرات

## Round 8 — تعديل درجات الامتحانات (عمليتان منفصلتان)

### إيه اللي بقى موجود
كل امتحان دلوقتي ليه **زرين تعديل مختلفين تمامًا** — كل واحد بصلاحية وسجل تدقيق مستقل:

**A) ✏️ تعديل الدرجة العظمى للامتحان (كل الامتحانات ← ✏️ تعديل العظمى)**
- بتغير الدرجة العظمى الكلية (مثال: 40 → 50) — والنظام بيظبط «لكل قسم» تلقائيًا (10 لكل قسم).
- **درجات الطلاب المسجلة مبتتغيرش خالص**: الطالب اللي 32/40 يفضل 32/50 — بس النسبة والدرجة النهائية بتتحسب من العظمى الجديدة.
- حماية تاريخية: لو فيه درجة طالب أعلى من العظمى الجديدة → التعديل بيرفض برسالة عربية واضحة.
- لازم العظمى تقبل القسمة على عدد الأقسام (50 على 5 = 10 ✓) — الواجهة بتقترحلك أقرب رقم صحيح.

**B) ✏️ تعديل درجة طالب واحد (كل الامتحانات ← 📋 الدرجات ← ✏️)**
- بتغير درجة طالب واحد بس — **الباقي مبيتأثروش والدرجة العظمى مبتتغيرش**.
- درجة الطالب بتتحدث في نفس مكانها (inline) من غير أي شاشة حاجزة — والواجهة بتتحدث فورًا من رد السيرفر (من غير reload).
- الأقسام بتتوزع نسبيًا على الدرجة الجديدة عشان التقارير تفضل متسقة (مجموع الأقسام = المجموع الجديد).
- نقاط الطالب بتتظبط بنفس معادلة الرصد الأصلية بالظبط (الفرق فوق/تحت نصف العظمى) — والفرق بيظهر في الـ toast (+3 نقطة مثلاً).

### حماية الغايبين (مفيش درجة عادية لغايب)
- **في الرصد (رصد امتحان جديد):** الطلبة الغايبين مش بيظهروش في شبكة الدرجات أصلًا — بيظهروا تحت في قسم منفصل «غايبين (مش هياخدوا درجات)» مع تلميح علّمهم حاضر الأول.
- **في التعديل:** صف الغايب بيظهر بشارة 🟡 «غائب» وزر التعديل مقفول — والسيرفر بيرفض أي محاولة برضه (مستحيل تتجاوز من المتصفح).
- لو الطالب اتعلّم حاضر بعدين (المسار المصرّح بيه) — بيبقى مؤهل للتعديل عادي.

### حماية التعديل المتزامن (اتنين بيعدلوا نفس الدرجة)
- كل درجة وكل امتحان بيه رقم نسخة (version). لو مدرّس تاني غيّر الدرجة قبل ما تحفظ انت:
  - حفظك بيرفض برسالة: «الدرجة اتعدّلت من مستخدم تاني — حدّث البيانات وجرب تاني».
  - الواجهة بتحمّل أحدث قيمة من السيرفر تلقائيًا — **مستحيل الكتابة القديمة تطغى على الجديدة**.

### سجل التدقيق (🧾 سجل التعديلات)
- كل تعديل بيتسجل في جدول append-only (مستحيل يتعدل أو يتمسح من الواجهة):
  - **من عدّل** (المدرّس/عضو مساحة العمل) · **إمتى** · **القيمة القديمة → الجديدة** · **السبب** (اختياري).
- من صفحة كل امتحان: زر «🧾 سجل التعديلات» بيعرض آخر 30 تعديل بالعربي.
- تظهر لحظيًا لو تعديل حصل من جهاز تاني والصفحة مفتوحة.

### الباك إند (Migration 038 — ملف SUPABASE-Round5.zip)
- `update_exam_max_score` و `update_student_exam_score`: دالتين RPC بكل التحقق من جهة السيرفر بس (العميل مش موثوق فيه):
  - الصلاحية بنفس النظام القائم (can_access_workspace — صاحب الامتحان/عضو مساحة عمله/جلسة دعم).
  - تحقق الحضور من attendance_records (حصة الامتحان أو يومه) — الغياب بيرفض.
  - الدرجة ≤ العظمى، مش سالبة، والقسمة على الأقسام سليمة.
  - التزامن بـ conditional update على version + قفل صف (FOR UPDATE).
- جدول `exam_score_audit_logs` (append-only — مفيش سياسات تعديل/حذف أصلًا).
- أعمدة `version` + `updated_at` على exams و exam_scores (البيانات القديمة بتاخد 1 تلقائيًا).
- كل حاجة idempotent — آمنة لإعادة التشغيل أي عدد مرات.

### الاختبارات (11/11 من المواصفات + إضافات)
كل سيناريو في المواصفات اتنفذ على PostgreSQL حقيقي: (1) تغيير العظمى مبيغيرش درجة الطالب، (2) تغيير درجة طالب مبيغيرش العظمى، (3) التسلسل الكامل 40→50→60، (4) درجة أكبر من العظمى بترفض، (5) الغايب بيرفض من السيرفر، (6) و(7) مدرّس من غير صلاحية بيرفض، (8) التعديل المتزامن القديم بيرفض، (9) و(10) البيانات بتستمر بعد الـ refresh، (11) سجل التدقيق للعمليتين. + اتساق الأقسام، حماية الفساد التاريخي، تعديل صفوف قديمة من غير رصد حضور، 42P13 guard.
- اختبار المتصفح الحقيقي: العمليتين اشتغلوا بنجاح + الغايب مقفول + التعارض اترفض بالرسالة + سجل التعديلات ظهر + صفر أخطاء console.

### إزاي تشغلها
1. استبدل ملفات الـ frontend (زي كل round).
2. شغّل `migration_038_exam_score_editing.sql` في Supabase ← SQL Editor ← Run (ملف SUPABASE-Round5.zip).
3. افتح الامتحانات ← كل الامتحانات — جرب ✏️ تعديل العظمى و 📋 الدرجات ← ✏️.

## Round 7 — تزامن لحظي + ضمان واتساب على أندرويد وآيفون

### 1) كل البيانات بتتحدث لحظيًا (المدرس)
- **كان المشكلة:** قناة Realtime القديمة كانت مسجلة على 3 جداول بس (students / behavior_logs / session_logs). أي تغيير في **الحصص (lesson_sessions)** أو **الحضور (attendance_records)** أو **الدرجات (exam_scores)** أو **الجدول (group_schedule)** أو **الإعدادات (teacher_settings)** أو **الرسائل العامة** ما كانش بيظهر غير بعد refresh كامل — وده بالظبط «السجل مش متزامن مع الحصص».
- **الحل:** نفس القناة دلوقتي مسجلة على كل الجداول التسعة. أي تغيير (من الجهاز ده أو من جهاز تاني أو من طابور الـ offline بعد رجوع النت) يحدّث الواجهة فورًا:
  - **الحصص:** فتح/تعديل/إنهاء/حذف درس يظهر لحظيًا، ولو درس اتفتح من جهاز تاني وهو مكتوب اسم مجموعته دلوقتي بيتadopted تلقائيًا (من غير ما يمسح اللي المدرس بيكيبه — حارس sessionHasChanges شغال).
  - **الحضور:** أي رصد (حاضر/غائب/واجب) يعدّل خريطة الحصة النشطة فورًا + يعيد حساب سلسلة الغياب المتتالية للطالب (استعلام مصغّر debounced).
  - **الدرجات:** النتائج الجديدة تظهر في الرسوم والتحذير الأكاديمي بدون reload.
  - **الإعدادات/الجدول:** تعديل مجموعة أو إعدادات من جهاز تاني ينزل فورًا + «مجموعات النهارده» بتتحدث لو اليوم انقلب والصفحة مفتوحة.
  - **دفتر اليوم (session_logs):** حماية من الكتابة القديمة اللي كانت بتطغى على سجل اليوم + دعم الحذف.
- **سجل حصص المجموعة (SessionHistoryModal):** بقى بيحدث نفسه لحظيًا وهو مفتوح — إنهاء حصة من أي جهاز يظهر فيه فورًا.

### 2) بوابة الطالب بتتحدث لحظيًا
- **كان:** بوابة الطالب (/qr/:token) بتعمل polling كل 10 ثواني بس.
- **الحل (portalRealtime.js):** قناة broadcast خفيفة `portal-refresh-<teacherId>`:
  - لوحة المدرس بتطلع «ping» (بدون أي بيانات طالب — مصدر + وقت بس) لحظة أي تغيير في أي جدول.
  - كل بوابة طالب مفتوحة بتسمع الـ ping ده وبتعمل refresh فوري (debounce 450ms + حماية من التكرار).
  - لو الطالب رجع للتاب/الأب بعد ما ضغط home — الصفحة بتتحدث فورًا (visibilitychange) — قبل كده الموبايل كان بيجمّد المؤقت والصفحة تفضل قديمة.
  - الـ polling كل 10 ثواني فضل كشكول أمان لأي مصدر تغيير تاني.
  - **الأمان:** الـ ping نفسه مفيهوش أي بيانات؛ البيانات بتيجي من نفس الـ RPC المؤمّن بالـ token — الـ ping بيقول «حدّث نفسك» بس.
- **لوحة الإعلانات:** بعد إرسال/حذف إعلان، كل البوابات المفتوحة بتشوفه خلال ثانية.

### 3) ضمان تقارير واتساب على أندرويد وآيفون (تحسين Round 4)
- كل مسارات الإرسال اتختبرت live على متصفح بمحاكاة iPhone 14 (window.open ممنوع — نفس سلوك iOS بعد async):
  - **الرسالة الفردية / التقرير الكامل / رسائل الإنذار والترقية:** بيوصلوا لواتساب (wa.me → التطبيق على الموبايل).
  - **شريط «فتح واتساب»:** ظاهر، لينك حقيقي `<a target="_blank">` — المسار المضمون على آيفون.
  - **طابور الرسائل:** أزرار `<a>` حقيقية زي ما هي.
- **الجديد — رسائل صادقة:** قبل كده لو آيفون حجب الفتح التلقائي، التطبيق كان بيكتب «تم الإرسال» رغم إن مفيش حاجة اتفتحت. دلوقتي `lastWhatsAppHandoffInfo()` بيقول للمدرس: «التقرير جاهز — لو واتساب ما اتفتحش لوحده، اضغط زر «فتح واتساب» اللي ظهر تحت» — على آيفون بس؛ أندرويد/الكمبيوتر بيكملوا «تم الإرسال» عادي لأن الفتح التلقائي بيشتغل عندهم فعلاً.
- إصلاح في وحدة البث: طبقات البينجات المتتالية في نفس المللي ثانية ما بقتش بتتلغي (كانت بتتشال كـ «مكررة» بالغلط).

### ملاحظات النشر
- **مفيش Migration جديد و مفيش خطوات Supabase جديدة** — كل جداول Realtime موجودة في الـ publication بالفعل (اتأكدنا live)، وقناة الـ broadcast بتشتغل بدون أي إعداد. اللي محتاجه بس: استبدال ملفات الواجهة (FE-fixed.zip) — وخطوات Round 6 لو لسه مش منفذة (Migration 037 + دالة الإشعارات).

## Round 6 — لوحة الإعلانات (التقارير — التبويب السادس)

### الجديد
- **📣 لوحة الإعلانات** — تبويب سادس جديد في قسم التقارير (بجانب: تقارير اليوم، الرسالة الجماعية، التحليلات، قوالب الرسائل، إرسال QR):
  - اكتب إعلان واحد → يوصل **كل طلابك** في نفس اللحظة 3 طرق:
    1. قسم **الإعلانات** في بوابة الطالب (`/qr/:token`) — فورًا.
    2. قسم **التنبيهات** في البوابة + عدّاد غير المقروء (زي أي تنبيه تاني).
    3. **إشعار على الموبايل حتى لو المتصفح مقفول** — إشعار Push حقيقي لكل جهاز فعّل الإشعارات من البوابة.
  - سجل بآخر 30 إعلان (عنوان/نص/تاريخ) + حذف بأكيد — والحذف بيشيل الإعلان من البوابة والتنبيهات كلها.
  - عدّادات نص (80 حرف للعنوان / 500 للرسالة) + بطاقة نتيجة بعد الإرسال: كام طالب وصل + كام جهاز هيترسل له إشعار.
- **فحص ذاتي للوحة** (زي نظام النسخ الاحتياطي): لوحة الإعلانات بتعرض حالة قاعدة البيانات (🟢 / في انتظار Migration 037) وحالة دالة الإشعارات (🟢 منشورة ومحدثة / ⚪ غير منشورة)، مع الخطوات المطلوبة بالعربي وأمر النسخ الجاهز — مش هتشتغل حاجة وهي صامتة.

### تحسينات البوابة (بوابة الطالب)
- **PWA كاملة**: ملف `manifest.webmanifest` + وسوم iPhone — الطالب/ولي الأمر يقدر يضيف الموقع للشاشة الرئيسية كتطبيق.
- **إشعارات iPhone**: على iOS الإشعار بيوصل بعد إضافة الموقع للشاشة الرئيسية — البوابة دلوقتي بتشرح الخطوات دي بالعربي لما المتصفح ما يدعمش الإشعارات مباشرة.
- **إشعارات عربية RTL**: خدمة الإشعارات (`sw.js`) بتعرض الإشعار من اليمين للشمال بالعربي، والضغط على الإشعار بيفتح **بوابة الطالب نفسها** (مش الصفحة الرئيسية) — لكل طالب رابطه الخاص.
- نسخة الخدمة اترفعت (v6) عشان الأجهزة اللي سجلت قبل كدا تاخد التحديث.

### الباك إند (يتطلب خطوتي نشر — ملف SUPABASE-Round4.zip)
1. **Migration 037** (`supabase/migrations/migration_037_announcement_board.sql`):
   - دالة `send_announcement`: إعلان + تنبيه لكل طالب + مهمة Push واحدة في معاملة واحدة (atomic).
   - إرسال جماعي `broadcast`: دالة Edge بتبعت لكل أجهزة الطلاب **دفعة واحدة** بدل مهمة لكل طالب.
   - `portal_url` لكل اشتراك إشعارات: الضغط على الإشعار بيفتح بوابة الطالب مباشرة (+ تعبئة تلقائية للاشتراكات القديمة).
   - منع التكرار: جسر تنبيهات المدرّس بيستثني إعلانات اللوحة (بدونها: المدرّس كان هيستقبل N حدث مكرر + الطالب إشعارين).
   - حذف الإعلان بيمسح تنبيهاته تلقائيًا (cascade) — ما بيبقاش رسايل قديمة عالقة.
   - سياسة قراءة جديدة `announcements_teacher_read` — قبل كدا سجل الإعلانات كان هيظهر فاضي للمدرّس.
   - كل حاجة idempotent — آمن لإعادة التشغيل.
2. **دالة `send-push-notification` محدّثة** (`supabase/functions/send-push-notification/`):
   - دعم الإرسال الجماعي + رابط بوابة الطالب لكل جهاز + إرسال متوازي (دفعات 20).
   - `ping` فحص صحة + معالجة CORS preflight (النسخة القديمة كانت بتقع عندها — عشان كدا مكانتش بتنفع للفحص من المتصفح).
   - المسارات القديمة (تنبيه المدرّس / تنبيه طالب واحد) زي ما هي بالظبط.

### إزاي تشغلها
1. شغّل `migration_037_announcement_board.sql` في Supabase ← SQL Editor ← Run.
2. انشر الدالة المحدّثة: `supabase functions deploy send-push-notification --project-ref pbsythpzncjoafpmijyd`
3. افتح التقارير ← لوحة الإعلانات — هتلاقي الحالة 🟢🟢.
(الإعلان نفسه بيشتغل في البوابة حتى قبل الخطوة 2 — بس الإشعار الجماعي على الموبايل محتاجها.)

### التحقق
- syntax-check (25 ملف) ✓ — run-all.sh 5/5 (14 integration + isolation + backup-edge + announcement-board 45 فحص) ✓ — oxlint 0 errors ✓ — npm run build ✓
- Migration 037: 30/30 statement على PostgreSQL حقيقي + إعادة تشغيل idempotent + 14 اختبار وظيفي (إرسال جماعي/فردي، منع تكرار الجسر، الروابط، الحذف المتسلسل، سياسات القراءة، كل الـ guards بالعربي) ✓
- دالة Edge: TS syntax ✓ + فحوصات المحتوى ✓
- المتصفح: اللوحة اتجربت لايف ضد قاعدة البيانات الحقيقية (حالات الانتقال + إرشادات) + مسار كامل بإجابات مزيفة (إرسال → نتيجة 25 طالب/7 أجهزة → السجل) + مقاس موبايل — 0 أخطاء console ✓

---
## Rounds 1–5 (موجز)
- R1: إعادة تطبيق إصلاحات (Tutorial/Dashboard/QR) بعد reset.
- R2: إصلاحات الحضور/البوابة (توجيه الدروس، مزامنة السجل، منع الدروس الوهمية، إصلاح التكرار).
- R3: أنظمة الإدارة الثلاثة (نسخ احتياطي + فرق + وصول دعم) + سجل التدقيق.
- R4: إصلاح WhatsApp/iOS (روابط wa.me + شريط تسليم حقيقي).
- R5: تشخيص ذاتي لدالة النسخ (ping + بانر إرشادي).
