# تشغيل NOKHBA على Windows

افتح Command Prompt داخل هذا المجلد نفسه، حيث يوجد package.json:

```bat
cd frontend
npm install
npm start
```

ثم افتح:

```text
http://localhost:3000/
```

إذا كنت داخل مجلد مختلف، نفّذ `dir package.json` وتأكد أن الملف ظاهر قبل تشغيل npm.
