# NOKHBA authentication fix

cd frontend
npm install
npm start

Open http://localhost:3000/

For production, set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in the frontend environment and add the deployed site URL to Supabase Authentication URL Configuration.
