-- ============================================================================
-- Migration 045 — التجربة المجانية: 14 يوم بدلًا من 7
-- (profiles.subscription_expires_at default → now() + interval '14 days')
-- سبتمبر 2026 — قرار معتمد من صاحب المنتج
-- ============================================================================
-- التشغيل: Supabase Dashboard → SQL Editor → الصق الملف كامل → Run
-- الملف idempotent: آمن لإعادة التشغيل، ولا يمس أو يعدّل أي بيانات قائمة.
--
-- الخلفية:
--   عند تسجيل مدرّس جديد، التريجر handle_new_user بينشئ صف في profiles
--   من غير ما يحدد subscription_expires_at، فبياخد الـ DEFAULT بتاع العمود
--   (كان now() + 7 days من schema.sql).
--   نموذج التجربة المعتمد دلوقتي: 14 يوم بكل المميزات الأساسية، بدون بطاقة،
--   والحساب بعد التجربة بيتوقف مؤقتًا (مش حذف) مع الاحتفاظ بالبيانات 30 يوم.
--
-- أثر التغيير:
--   - أي تسجيل جديد بعد تشغيل الملف بياخد 14 يوم من لحظة التسجيل.
--   - الحسابات الموجودة أصلًا: صفر تأثير (الـ DEFAULT بيخص التسجيلات الجديدة بس).
--   - الأدمن زه ما زال يقدر يعدّل/يمدّد أي اشتراك يدويًا من لوحة الأدمن
--     (تفعيل/تمديد) — ده مصدر الحقيقة الفعلي لتجربة كل حساب.
--
-- ملحوظة تشغيلية مهمة:
--   لما تفعّل حساب جديد من لوحة الأدمن، اتأكد إنك بتمدد الاشتراك 14 يوم
--   (تفعيل/تمديد → تمديد بأيام → 14) — لأن فترة التجربة بتعدّ من لحظة
--   التسجيل مش من لحظة التفعيل، وممكن صاحب الحساب يكون استنى أيام
--   قبل التفعيل.
-- ============================================================================

alter table public.profiles
  alter column subscription_expires_at
  set default (now() + interval '14 days');

-- تحقق سريع (اختياري): الـ DEFAULT الجديد لازم يظهر في ناتج الأمر ده
-- select column_default from information_schema.columns
--  where table_schema = 'public' and table_name = 'profiles'
--    and column_name = 'subscription_expires_at';
