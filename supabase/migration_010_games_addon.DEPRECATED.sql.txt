-- ============================================================================
-- ⚠️ ملف قديم غير مستخدم — متشغّلوش
-- ============================================================================
-- الملف ده كان محاولة مبكرة لميزة قفل/فتح الألعاب، بعمل عمودين على جدول
-- profiles (games_addon_status, games_addon_expires_at).
--
-- النظام الفعلي المستخدم دلوقتي في الكود (AdminDashboard.jsx, GamesHubModal.jsx)
-- بيعتمد على جدول منفصل اسمه feature_unlocks، الموجود في:
--     migration_010_feature_unlocks.sql
--
-- لو كنت شغّلت الملف ده قبل كده بالغلط، معلش — مش هيسبب أي مشكلة، بس
-- العمودين دول (games_addon_status, games_addon_expires_at) مش بيتقروا
-- ولا بيتكتبوا من أي حتة في الكود، فتقدر تتجاهلهم أو تمسحهم من غير قلق:
--
--   alter table profiles drop column if exists games_addon_status;
--   alter table profiles drop column if exists games_addon_expires_at;
--
-- (اختياري، مش لازم — بس عشان تنظيف قاعدة البيانات لو حابب)
-- ============================================================================
